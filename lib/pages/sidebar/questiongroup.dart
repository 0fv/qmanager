import 'package:flutter/material.dart';

class QuestionGroup extends StatelessWidget {
  const QuestionGroup({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("gr"),
    );
  }
}